// Entorno de produccion.
// El valor de apiUrl debe ser reemplazado durante el deploy (ej: sed o variable de entorno en CI/CD).
export const environment = {
  production: true,
  apiUrl: 'https://api.iglesia.example.com'
};
